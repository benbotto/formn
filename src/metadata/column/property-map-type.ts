/**
 * See [[ColumnStore]].  This is an object that has all the properties of a
 * [[Table]]-decorated class.  Each property maps to the name of the property
 * on the class. E.g. {id: 'id', name: 'name'}.
 */
export type PropertyMapType = {[key: string]: string};
